[project]
name = "alayaos-connectors"
version = "0.1.0"
description = "AlayaOS source connectors"
requires-python = ">=3.13"
dependencies = [
    "alayaos-core",
]

[tool.uv.sources]
alayaos-core = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
